header
    nav
        a(href='/') All tours
    div
        img(src='/img/logo-white.png' alt='')
    nav
        if user
            button Log out
            a(href='/me')
                img(src=`/img/users/${user.photo}` alt=`Photo of ${user.photo}`)
                span= user.name.split(' ')[0]
        else
            a(href='/login') Log In
            a(href='/signup') Sign Up